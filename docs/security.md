# امنیت و احراز هویت

- کلید API و Secret داخل سورس Flutter یا GitHub ذخیره نمی‌شوند.
- اپ موبایل در نسخه نهایی از Login Session/OAuth و Secure Storage استفاده می‌کند.
- تمام Endpointها به‌صورت پیش‌فرض Guest را نمی‌پذیرند.
- مجوزهای Frappe پیش از هر خواندن یا نوشتن بررسی می‌شوند.
- خطاهای داخلی و Stack Trace به کاربر نمایش داده نمی‌شوند.
- لاگ تغییرات سرفصل و تفصیلی فعال است.
- حذف اطلاعات مالی به غیرفعال‌سازی تبدیل می‌شود.

